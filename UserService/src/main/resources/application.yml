server:
  port: 8084

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/userdb
    username: root
    password: yourpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
